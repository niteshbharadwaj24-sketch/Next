use nitesh if possible

CREATE TABLE employee(
ID INT primary key,
name varchar(50),
Age int not null,
salary int default 25000
);

drop table employee ALLOCATE

select * from employee


show  databases;

exec databases

create database  IF NOT Exists nitesh

insert into employee values(1, 'litesh', 29, 1500000);
insert into employee(id,name,age) values(2,'sp',91);
insert into student values(3,'PK',29);

select * from employee


just for test
